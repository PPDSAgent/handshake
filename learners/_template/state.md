# State — TEMPLATE

- current_unit: 0
- streak: 0
- last_session: never
- asl_ms_speed: not yet measured

*(For a `lite`-surface learner, add `- last_relay: never` — their state here is a
snapshot from the most recent export line in `learners/shared/relay.md`, written by
`/checkin`. Treat a stale `last_relay` as "unknown", never as "they stopped".)*
